'use client'
import Link from 'next/link'
import { useEffect,useState } from 'react'
export default function History(){const [items,setItems]=useState<{question:string,answer:string,date:string}[]>([]);useEffect(()=>setItems(JSON.parse(localStorage.getItem('solveai-history')||'[]')),[]);return <main className="min-h-[calc(100vh-73px)] bg-mist px-5 py-12"><div className="mx-auto max-w-4xl"><h1 className="text-4xl font-bold">Your history</h1><p className="mt-3 text-slate-500">Your recent solutions are stored securely in this browser for now.</p>{items.length===0?<div className="mt-10 rounded-3xl border border-dashed bg-white p-12 text-center"><p className="text-slate-500">No solutions yet.</p><Link href="/solve" className="mt-5 inline-block rounded-full bg-ink px-5 py-3 font-semibold text-white">Solve your first problem</Link></div>:<div className="mt-8 space-y-3">{items.map((item,i)=><Link href="/solve" key={i} className="block rounded-2xl border bg-white p-5 hover:border-brand"><p className="font-semibold">{item.question}</p><p className="mt-2 line-clamp-2 text-sm text-slate-500">{item.answer}</p><time className="mt-3 block text-xs text-slate-400">{new Date(item.date).toLocaleString()}</time></Link>)}</div>}</div></main>}
